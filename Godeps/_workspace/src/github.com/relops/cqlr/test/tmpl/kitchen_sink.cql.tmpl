-- THIS FILE WAS AUTOGENERATED - ANY EDITS TO THIS WILL BE LOST WHEN IT IS REGENERATED

CREATE TABLE kitchen_sink
(
    id int,

    {{ range $_, $t := .types }}
    {{ $t.Pre }}column {{ $t.Cql }},
    {{ end }}

    {{ range $_, $t := .types }}
    {{ $t.Pre }}listcolumn list<{{ $t.Cql }}>,
    {{ end }}

    {{ $inner := .types }}
	{{ $outer := .types }}

	{{ range $_, $ot := $outer }}
	{{ range $_, $it := $inner }}
	{{ if ne $ot.Pre "Bytes" }}
	{{ $ot.Pre }}{{ $it.Pre }}column map<{{ $ot.Cql }},{{ $it.Cql }}>,
	{{ end }}{{ end }}{{ end }}

    PRIMARY KEY (id)
);